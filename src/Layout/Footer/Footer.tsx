import { FC } from 'react';
import Navigation from '../../components/Navigation';

const Footer: FC = () => {
  return (
    <footer>
      <Navigation />
    </footer>
  );
};

export default Footer;
